# PC Follow v0.1.14

## 主な更新内容

### Host / Client 対応
- 多重起動・2PC環境向けに **Host / Client** 通信モードを追加。
- **Host**：Lv上げ対象側。
- **Client**：PL・追従側。
- **Solo**：PL側だけで使用する単体モード。Lv上げ対象側ではPC Followの起動不要。
- Host / Client利用時は2キャラともPC Followを起動。

### Host → Client 同期
- Hostがテレポした場所へClientもテレポ。
- ジャンプ、スプリント、マウント乗車／降車、飛行状態に対応。
- Hostを自動認識して追従。
- Host側からClientの追従開始／停止が可能。

### RSR連携
- ClientはRSRを使用して戦闘。
- 追従開始時にRSRをON。
- 追従停止時にRSRをOFF。

### 蘇生
- Client側で蘇生用ギアセット番号を設定可能。
- Host死亡時に蘇生用ギアセットへ変更して蘇生。
- 蘇生後は元のJOB／ギアセットへ戻し、追従とRSRを再開。

### 賢者バリア
- 賢者モード時にHostへバリアを付与。
- HostのHPが0の場合はバリア付与を行わないよう修正。
- 復活後、必要に応じてバリアを再付与。

### Host先制処理
- 通信モードがHostのとき、**Host操作タブ → 先制設定** から使用可能。
- 現在JOBに応じた敵対アクションを候補表示。
- 手動入力したアクションを優先可能。
- 距離取得にラグがあるため、先制動作の**試行開始距離**をユーザー側で指定可能。
- 試行開始距離はJOBごとに保存。
- 短い間隔で実行を試行し、ゲーム側が受け付けた時点で完了。

### 自動ターゲット／NG MOB
- HP満タンの敵を自動ターゲット候補に使用。
- HPが減っている敵、NG登録した敵は自動候補から除外。
- 自動選択した対象より近い有効な敵がいれば自動で切替。
- 手動で選択した敵には自動切替を適用しない。
- 敵をターゲットして登録ボタンを押すことでNG MOB登録が可能。
- 自動ターゲットしない場合は、現在の敵ターゲットを一度外すことで再選択可能。

### UI
- 表記を **Solo / Host / Client** に整理。
- Helpタブを追加。
- Dalamud標準のWindowSystemへ移行し、タイトルバーの標準ウィンドウ設定に対応。
